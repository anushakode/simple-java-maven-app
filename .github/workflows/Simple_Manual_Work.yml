name: SimpleManualWork
on:
  workflow_dispatch:
  
  
  #push:
    #branches:
     # - master
     # - manual_work
      #- manual_work1
  
jobs:
  
  Enable-for-feature-branch:
    if: github.ref != 'refs/heads/master'
    runs-on: ubuntu-latest
 
#jobs:
   #This-worflow-is-enable-for-feature-brach-only:
   # if: github.ref != 'refs/heads/master
    #runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      - name: anusha
        run: |
            echo "this workflow enable for feature branch"
        

      
